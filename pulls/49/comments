**⚠️ Updated requirement from owner — please read before continuing:**

**DO NOT modify `frontend/src/middleware.ts` at all.** Keep the existing HTTP Basic Auth logic in that file exactly as it is in `main`. Do not remove, simplify, or alter it.

The `middleware.ts` file should remain unchanged. The Basic Auth gate is intentional.

**Only implement the following changes:**
1. Create `frontend/src/hooks/useUser.ts` — shared auth hook
2. Rewrite `frontend/src/components/landing/Navbar.tsx` — add user avatar dropdown for authenticated users
3. Modify `frontend/src/components/dashboard/Dashboard.tsx` — replace MOCK_USER_ID with real Supabase session user ID; load real `chars_remaining` from `public.users`
4. Modify `frontend/src/lib/api.ts` — remove hardcoded `user_id: "mock-user-123"` from `generateContent` and `streamGenerateContent`
5. Create `frontend/src/app/auth/callback/route.ts` — OAuth code exchange callback
6. Create `frontend/src/app/api/auth/signout/route.ts` — server-side sign out

Do NOT touch `frontend/src/middleware.ts`.